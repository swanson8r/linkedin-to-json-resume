language: node_js
node_js:
  - "node"
before_script:
  - npm install
branches:
  only:
    - main
    - /^greenkeeper/.*$/
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: main
  local-dir: dist
